/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // The simulation layer is client-side; the console is fully self-contained for testing.
};
export default nextConfig;
